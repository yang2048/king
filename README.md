# YK （dev）

```
A) Service / DAO 层方法命名规约
1 ）获取单个对象的方法用 get 做前缀。
2 ）获取多个对象的方法用get开头，实体类名加List [eg: getOrderListByName()]
3 ）获取统计值的方法用get开头，实体类名加Count
4 ）插入的方法用 save（推荐）或 insert 做前缀。
5 ）删除的方法用 remove（推荐）或 delete 做前缀。
6 ）修改的方法用 update 做前缀。
```

```$xslt
登录、注册、密码重置
角色添加

JWT,Shiro 集成
登录注册接口，
前端框架选型
后台数据列表接口
前端数据展示

```